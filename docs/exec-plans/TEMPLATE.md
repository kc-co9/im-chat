# <任务名称>实施计划

> 仅用于跨模块、跨会话、高风险链路、迁移、公共契约或存在开放决策的工作。小型单文件维护按 `docs/PLANS.md` 执行，无需复制本模板。
>
> Coding Agent 使用 `superpowers:subagent-driven-development`（有子代理时）或 `superpowers:executing-plans` 执行本计划。详细步骤必须使用 `- [ ]`/`- [x]` 跟踪，完成后立即勾选；除非用户明确要求，不创建提交。

## Sprint Contract

- 目标：
- 范围：
- 不做事项：
- 验收标准：
- 风险链路：
- 依赖输入与所需权限：

## 事实源

- 设计或产品规格：
- 根与局部 Architecture：
- API、协议或数据契约：
- 相关代码入口：

## 验证分层

| 层级 | 命令或证据 | 必须/可选 | 当前结果 |
|---|---|---|---|
| RED fixture | - | 按行为变更 | 未运行 |
| 聚焦测试 | - | 按风险 | 未运行 |
| 架构或契约 | - | 按变更 | 未运行 |
| 行为或运行环境 | - | 按变更 | 未运行 |
| 完整门禁 | `./scripts/verify.sh clean`、`./scripts/verify.sh full` | 必须 | 未运行 |

## 任务状态

同一时间至多一项任务为 `active`。状态只能使用 `not_started`、`active`、`blocked`、`passing`。

| ID | 行为目标 | 范围 | 状态 | 验证证据 | 阻塞/备注 |
|---|---|---|---|---|---|
| T1 | - | - | `active` | - | - |

## 详细任务

### T1 <行为目标>

输入与边界：

文件范围：

- [ ] 新增最小 RED 测试或契约 fixture。
- [ ] 运行 `<精确命令>`，预期因 `<缺失行为>` 失败。
- [ ] 在 `<准确文件路径>` 实现最小行为。
- [ ] 运行 `<精确命令>`，预期通过，并执行受影响的回归测试。
- [ ] 更新所属 README、Architecture、设计或 Harness 规则；不适用时写明原因。

完成条件：

## 恢复状态

- 当前任务：T1。
- 已完成：
- 阻塞项：`none`。
- 下一步：
- 不要修改：

## 回滚与残余风险

- 回滚：
- 未验证路径：
- 已接受但需跟踪的风险：没有则填写 `none`；需要延期治理时登记到 `docs/exec-plans/tech-debt-tracker.md`。
